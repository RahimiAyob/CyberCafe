<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="/TopUpServlet (snapshot).java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/TopUpServlet (snapshot).java" />
              <option name="originalContent" value="package com.project.cybercafe;&#10;&#10;import java.io.IOException;&#10;import java.sql.Connection;&#10;import java.sql.PreparedStatement;&#10;import java.sql.ResultSet;&#10;import java.sql.SQLException;&#10;import jakarta.servlet.ServletException;&#10;import jakarta.servlet.annotation.WebServlet;&#10;import jakarta.servlet.http.HttpServlet;&#10;import jakarta.servlet.http.HttpServletRequest;&#10;import jakarta.servlet.http.HttpServletResponse;&#10;import jakarta.servlet.http.HttpSession;&#10;&#10;@WebServlet(&quot;/TopUpServlet&quot;)&#10;public class TopUpServlet extends HttpServlet {&#10;&#10;    // handle our card clicks via GET requests&#10;    @Override&#10;    protected void doGet(HttpServletRequest request, HttpServletResponse response)&#10;            throws ServletException, IOException {&#10;&#10;        HttpSession session = request.getSession(false);&#10;        String minutesParam = request.getParameter(&quot;minutes&quot;);&#10;&#10;        if (session != null &amp;&amp; session.getAttribute(&quot;LOGGED_IN_USER&quot;) != null &amp;&amp; minutesParam != null) {&#10;            String username = (String) session.getAttribute(&quot;LOGGED_IN_USER&quot;);&#10;            int minutesToAdd = Integer.parseInt(minutesParam);&#10;&#10;            try (Connection conn = DatabaseConnection.getConnection()) {&#10;                // 1. update the database balance&#10;                String topUpSql = &quot;UPDATE USERS SET BANKED_MINUTES = BANKED_MINUTES + ? WHERE USERNAME = ?&quot;;&#10;                try (PreparedStatement topUpStmt = conn.prepareStatement(topUpSql)) {&#10;                    topUpStmt.setInt(1, minutesToAdd);&#10;                    topUpStmt.setString(2, username);&#10;                    topUpStmt.executeUpdate();&#10;                }&#10;&#10;                // 2. dynamically extend their live active session clock if they are already on the dashboard&#10;                Long currentEndTime = (Long) session.getAttribute(&quot;SESSION_END_TIME&quot;);&#10;                if (currentEndTime != null) {&#10;                    long updatedEndTime = currentEndTime + (minutesToAdd * 60L * 1000L);&#10;                    session.setAttribute(&quot;SESSION_END_TIME&quot;, updatedEndTime);&#10;                }&#10;&#10;                response.sendRedirect(&quot;member_billing.jsp?status=success&quot;);&#10;                return;&#10;&#10;            } catch (SQLException e) {&#10;                e.printStackTrace();&#10;            }&#10;        }&#10;        response.sendRedirect(&quot;login.jsp&quot;);&#10;    }&#10;&#10;    // keep the old POST mode intact just in case you use a standalone kiosk terminal form later&#10;    @Override&#10;    protected void doPost(HttpServletRequest request, HttpServletResponse response)&#10;            throws ServletException, IOException {&#10;        String usernameParam = request.getParameter(&quot;username&quot;);&#10;        String minutesParam = request.getParameter(&quot;minutes&quot;);&#10;&#10;        if (usernameParam != null &amp;&amp; minutesParam != null) {&#10;            try (Connection conn = DatabaseConnection.getConnection()) {&#10;                String topUpSql = &quot;UPDATE USERS SET BANKED_MINUTES = BANKED_MINUTES + ? WHERE USERNAME = ?&quot;;&#10;                try (PreparedStatement topUpStmt = conn.prepareStatement(topUpSql)) {&#10;                    topUpStmt.setInt(1, Integer.parseInt(minutesParam));&#10;                    topUpStmt.setString(2, usernameParam.trim());&#10;                    topUpStmt.executeUpdate();&#10;                }&#10;                response.sendRedirect(&quot;topup.jsp?status=success&quot;);&#10;                return;&#10;            } catch (SQLException e) {&#10;                e.printStackTrace();&#10;            }&#10;        }&#10;        response.sendRedirect(&quot;topup.jsp?status=db_error&quot;);&#10;    }&#10;}" />
              <option name="updatedContent" value="package com.project.cybercafe;&#10;&#10;import java.io.IOException;&#10;import java.sql.Connection;&#10;import java.sql.PreparedStatement;&#10;import java.sql.ResultSet;&#10;import java.sql.SQLException;&#10;import jakarta.servlet.ServletException;&#10;import jakarta.servlet.annotation.WebServlet;&#10;import jakarta.servlet.http.HttpServlet;&#10;import jakarta.servlet.http.HttpServletRequest;&#10;import jakarta.servlet.http.HttpServletResponse;&#10;import jakarta.servlet.http.HttpSession;&#10;&#10;@WebServlet(&quot;/TopUpServlet&quot;)&#10;public class TopUpServlet extends HttpServlet {&#10;&#10;    // handle our card clicks via GET requests&#10;    @Override&#10;    protected void doGet(HttpServletRequest request, HttpServletResponse response)&#10;            throws ServletException, IOException {&#10;&#10;        HttpSession session = request.getSession(false);&#10;        String minutesParam = request.getParameter(&quot;minutes&quot;);&#10;&#10;        if (session != null &amp;&amp; session.getAttribute(&quot;LOGGED_IN_USER&quot;) != null &amp;&amp; minutesParam != null) {&#10;            String username = (String) session.getAttribute(&quot;LOGGED_IN_USER&quot;);&#10;            Integer userId = (Integer) session.getAttribute(&quot;USER_ID&quot;);&#10;            int minutesToAdd = Integer.parseInt(minutesParam);&#10;            double amount = calculatePrice(minutesToAdd);&#10;&#10;            try (Connection conn = DatabaseConnection.getConnection()) {&#10;                // 1. update the database balance&#10;                String topUpSql = &quot;UPDATE USERS SET BANKED_MINUTES = BANKED_MINUTES + ? WHERE USERNAME = ?&quot;;&#10;                try (PreparedStatement topUpStmt = conn.prepareStatement(topUpSql)) {&#10;                    topUpStmt.setInt(1, minutesToAdd);&#10;                    topUpStmt.setString(2, username);&#10;                    topUpStmt.executeUpdate();&#10;                }&#10;&#10;                // 2. log the transaction to TRANSACTION table (if user is a member, not guest)&#10;                if (userId != null) {&#10;                    String transactionSql = &quot;INSERT INTO TRANSACTION (USER_ID, SESSION_ID, AMOUNT) VALUES (?, NULL, ?)&quot;;&#10;                    try (PreparedStatement tranStmt = conn.prepareStatement(transactionSql)) {&#10;                        tranStmt.setInt(1, userId);&#10;                        tranStmt.setDouble(2, amount);&#10;                        tranStmt.executeUpdate();&#10;                    }&#10;                }&#10;&#10;                // 3. dynamically extend their live active session clock if they are already on the dashboard&#10;                Long currentEndTime = (Long) session.getAttribute(&quot;SESSION_END_TIME&quot;);&#10;                if (currentEndTime != null) {&#10;                    long updatedEndTime = currentEndTime + (minutesToAdd * 60L * 1000L);&#10;                    session.setAttribute(&quot;SESSION_END_TIME&quot;, updatedEndTime);&#10;                }&#10;&#10;                response.sendRedirect(&quot;member_billing.jsp?status=success&quot;);&#10;                return;&#10;&#10;            } catch (SQLException e) {&#10;                e.printStackTrace();&#10;            }&#10;        }&#10;        response.sendRedirect(&quot;login.jsp&quot;);&#10;    }&#10;&#10;    // keep the old POST mode intact just in case you use a standalone kiosk terminal form later&#10;    @Override&#10;    protected void doPost(HttpServletRequest request, HttpServletResponse response)&#10;            throws ServletException, IOException {&#10;        String usernameParam = request.getParameter(&quot;username&quot;);&#10;        String minutesParam = request.getParameter(&quot;minutes&quot;);&#10;&#10;        if (usernameParam != null &amp;&amp; minutesParam != null) {&#10;            try (Connection conn = DatabaseConnection.getConnection()) {&#10;                String topUpSql = &quot;UPDATE USERS SET BANKED_MINUTES = BANKED_MINUTES + ? WHERE USERNAME = ?&quot;;&#10;                try (PreparedStatement topUpStmt = conn.prepareStatement(topUpSql)) {&#10;                    topUpStmt.setInt(1, Integer.parseInt(minutesParam));&#10;                    topUpStmt.setString(2, usernameParam.trim());&#10;                    topUpStmt.executeUpdate();&#10;                }&#10;                response.sendRedirect(&quot;topup.jsp?status=success&quot;);&#10;                return;&#10;            } catch (SQLException e) {&#10;                e.printStackTrace();&#10;            }&#10;        }&#10;        response.sendRedirect(&quot;topup.jsp?status=db_error&quot;);&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>